name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      # تحديث: استخدام الإصدار v4
      - name: Checkout Source
        uses: actions/checkout@v4

      # تحديث: استخدام الإصدار v4
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # تحديث: استخدام الإصدار v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Cordova
        run: npm install -g cordova

      - name: Prepare Project Structure
        run: |
          mkdir www
          mv index.html style.css script.js manifest.json sw.js adhan.mp3 www/
          mkdir -p res/android/icon/
          touch res/android/icon/ldpi.png 

      - name: Install Dependencies
        run: npm install

      - name: Add Android Platform
        run: cordova platform add android@12.0.0

      - name: Build APK (Debug)
        run: cordova build android --debug --device

      # تحديث: هذا هو الجزء الذي سبب المشكلة، تم تغييره إلى v4
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Moazen-App
          path: platforms/android/app/build/outputs/apk/debug/app-debug.apk
